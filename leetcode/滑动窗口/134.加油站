# 134. 加油站

## 题目

在一条环路上有 `n` 个加油站，其中第 `i` 个加油站有汽油 `gas[i]` 升。

你有一辆油箱容量无限的的汽车，从第 `i` 个加油站开往第 `i+1` 个加油站需要消耗汽油 `cost[i]` 升。你从其中的一个加油站出发，开始时油箱为空。

给定两个整数数组 `gas` 和 `cost` ，如果你可以按顺序绕环路行驶一周，则返回出发时加油站的编号，否则返回 `-1` 。如果存在解，则 **保证** 它是 **唯一** 的。

**示例 1:**

```
输入: gas = [1,2,3,4,5], cost = [3,4,5,1,2]
输出: 3
解释:
从 3 号加油站(索引为 3 处)出发，可获得 4 升汽油。此时油箱有 = 0 + 4 = 4 升汽油
开往 4 号加油站，此时油箱有 4 - 1 + 5 = 8 升汽油
开往 0 号加油站，此时油箱有 8 - 2 + 1 = 7 升汽油
开往 1 号加油站，此时油箱有 7 - 3 + 2 = 6 升汽油
开往 2 号加油站，此时油箱有 6 - 4 + 3 = 5 升汽油
开往 3 号加油站，你需要消耗 5 升汽油，正好足够你返回到 3 号加油站。
因此，3 可为起始索引。
```

**示例 2:**

```
输入: gas = [2,3,4], cost = [3,4,3]
输出: -1
解释:
你不能从 0 号或 1 号加油站出发，因为没有足够的汽油可以让你行驶到下一个加油站。
我们从 2 号加油站出发，可以获得 4 升汽油。 此时油箱有 = 0 + 4 = 4 升汽油
开往 0 号加油站，此时油箱有 4 - 3 + 2 = 3 升汽油
开往 1 号加油站，此时油箱有 3 - 3 + 3 = 3 升汽油
你无法返回 2 号加油站，因为返程需要消耗 4 升汽油，但是你的油箱只有 3 升汽油。
因此，无论怎样，你都不可能绕环路行驶一周。
```

**提示:**

- `n == gas.length == cost.length`
- `1 <= n <= 105`
- `0 <= gas[i], cost[i] <= 104`
- 输入保证答案唯一。

## 解题思路

本题要求找到一个起点，使得汽车能够绕环路行驶一周。

最容易想到的方法是**枚举起点 + 模拟行驶过程**。

对于每一个可能的起点，都尝试一直向后行驶，并维护当前油箱中的剩余油量：

- 到达当前加油站后，获得 `gas[i]` 升汽油；
- 驶向下一站时，消耗 `cost[i]` 升汽油；
- 若剩余油量始终大于等于 0，则可以继续前进；
- 一旦剩余油量小于 0，则说明当前起点无法完成一圈。

不过，直接暴力会导致时间复杂度达到 **O(n²)**。

实际上可以利用一个重要性质进行优化：

> 如果从起点 `l` 出发，在到达 `r` 时第一次无法继续前进，那么区间 `[l,r]` 内任意一个加油站作为起点，都不可能到达 `r+1`。

因此，当从 `l` 出发失败后，可以直接将下一次起点更新为 `r+1`，跳过中间所有已经证明不可能成为答案的位置，从而避免大量重复计算。

---

## 算法流程

定义两个指针：

- `l`：当前尝试的起点。
- `r`：当前已经行驶到的位置。

同时维护一个变量：

- `sum`：当前油箱中的剩余油量。

具体过程如下：

1. 枚举当前起点 `l`。

2. 初始化：

   - `r = l`
   - `sum = 0`

3. 不断向后模拟：

   - 判断当前剩余油量是否能够驶向下一站：

     ```cpp
     sum + gas[r] - cost[r] >= 0
     ```

   - 若可以，则更新剩余油量，并继续向后移动 `r`。

4. 若已经成功行驶 `n` 个加油站，则说明已经绕环一周，返回当前起点 `l`。

5. 若中途失败，则根据上述性质，直接令

   ```cpp
   l = r + 1
   ```

   跳过区间 `[l,r]` 内所有位置，继续寻找新的起点。

6. 若所有位置均尝试结束仍未找到合法起点，则返回 `-1`。

---

## 为什么可以直接跳到 `r+1`？

假设从 `l` 出发，到达 `r` 时第一次失败。

设区间

```
l ...... k ...... r
```

其中 `k` 为 `l` 与 `r` 之间任意一个位置。

由于：

- 从 `l` 能够一直走到 `k`；
- 但最终仍然无法走过 `r`。

说明：

```
sum(l→r) < 0
```

而从 `k` 开始：

由于少走了前面的部分路程，同时也少获得了对应的汽油，因此到达 `r` 时的剩余油量一定不会比从 `l` 出发更多。

因此：

- `k` 同样无法通过 `r`；
- 即区间 `[l,r]` 内所有点都不可能成为合法起点。

所以可以直接跳到 `r+1`。

---

## 代码

```cpp
class Solution {
public:
    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
        int n = gas.size();

        for (int l = 0, r = 0, sum; l < n; l = r + 1, r = l) {
            sum = 0;

            while (sum + gas[r % n] - cost[r % n] >= 0) {
                if (r - l + 1 == n)
                    return l;

                sum += gas[r % n] - cost[r % n];
                r++;
            }
        }

        return -1;
    }
};
```

---

## 复杂度分析

- **时间复杂度：O(n)**

  虽然存在两层循环，但由于每个加油站最多只会作为失败位置一次，被跳过后不会再次访问，因此两个指针总体只遍历数组一次。

- **空间复杂度：O(1)**

  仅使用了若干辅助变量，没有使用额外空间。

---

## 核心思想

本题的关键并不是模拟，而是利用下面这一贪心性质：

> **如果从起点 `l` 无法到达 `r+1`，那么区间 `[l,r]` 内的任何一个位置都不可能作为合法起点。**

正是利用这一性质，才能在失败后直接跳过一整段区间，将时间复杂度从 **O(n²)** 优化到 **O(n)**。
